version: '3.9'

services:
  frontend:
    build:
      context: .
      dockerfile: DockerFile.dev
    container_name: nextjs-frontend-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev
